namespace THNETII.AzureAcs.Client.Metadata
{
    internal static class LogMessages
    {
        internal const string ACS2001 = nameof(ACS2001) + ": Deserializing the string: '{0}' obtained from metadata endpoint into a " + nameof(AcsInstanceMetadata) + " object.";
    }
}
